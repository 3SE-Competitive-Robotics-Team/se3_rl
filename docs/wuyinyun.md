# wuyinyun 训练机器运维笔记（历史归档）

> `codex/xyh` 当前分支没有 `wuyinyun` 远程训练服务器。当前只使用 `a800`（A800 多卡训练）和 `gpufree`（L40S 单卡训练 / smoke / 备用）。

本文只保留归档入口，避免旧命令被误当成当前操作手册。不要把 `wuyinyun` 作为 SSH、代理、训练、checkpoint 拉取、Rerun 录制或默认示例目标。

当前远程训练入口：

- A800：见 `.agents/skills/remote-dev-se3/machines/a800.md`
- gpufree：见 `.agents/skills/remote-dev-se3/machines/gpufree.md`
- 通用远程运维规则：见 `.agents/skills/remote-dev-se3/SKILL.md`
