# wuyingyun 训练机器运维笔记

> `wuyingyun` 是无影云 RTX 5880 单卡训练机。新文档、脚本和 SSH 配置统一使用 `wuyingyun`。

本文只保留入口说明，具体连接参数、依赖坑和训练操作以 `.agents/skills/remote-dev-se3/machines/wuyingyun.md` 为准。没有明确指定机器时，默认仍优先使用 `a800` 或 `gpufree`。

当前远程训练入口：

- A800：见 `.agents/skills/remote-dev-se3/machines/a800.md`
- gpufree：见 `.agents/skills/remote-dev-se3/machines/gpufree.md`
- wuyingyun：见 `.agents/skills/remote-dev-se3/machines/wuyingyun.md`
- 通用远程运维规则：见 `.agents/skills/remote-dev-se3/SKILL.md`
