# prek.toml — 提交前钩子配置
# prek 在 git commit 前自动执行格式化和检查

[[hooks]]
name = "ruff-format"
cmd = ["uv", "run", "ruff", "format", "."]
stages = ["pre-commit"]

[[hooks]]
name = "ruff-check"
cmd = ["uv", "run", "ruff", "check", ".", "--fix"]
stages = ["pre-commit"]
